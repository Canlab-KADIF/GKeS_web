import 'package:flutter/material.dart';

class UserHistorySession extends StatelessWidget {
    const UserHistorySession({super.key});

    @override
    Widget build(BuildContext context) {
        return Container(
            color: Colors.blue,
            child: Text('User History Session'),
        );
    }
}