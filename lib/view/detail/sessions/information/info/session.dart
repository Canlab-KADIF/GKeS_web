import 'package:flutter/material.dart';

class InfoSession extends StatelessWidget {
    const InfoSession({super.key});

    @override
    Widget build(BuildContext context) {
        return Container(
            color: Colors.blue,
            child: Text('Info Session'),
        );
    }
}