import 'package:flutter/material.dart';

class MetaSession extends StatelessWidget {
    const MetaSession({super.key});

    @override
    Widget build(BuildContext context) {
        return Container(
            color: Colors.blue,
            child: Text('Meta Session'),
        );
    }
}