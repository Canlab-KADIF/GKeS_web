import 'package:flutter/material.dart';

class LogHeaderSession extends StatelessWidget {
    const LogHeaderSession({super.key});

    @override
    Widget build(BuildContext context) {
        return Container(
            color: Colors.blue,
            child: Text('Log Header Session'),
        );
    }
}