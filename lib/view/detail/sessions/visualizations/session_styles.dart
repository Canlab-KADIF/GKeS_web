import 'package:flutter/material.dart';

class VisualizationsSessionStyle {
    static const EdgeInsets padding = EdgeInsets.only(left: 32, right: 32, top: 20, bottom: 20);

    static const double video_component_weight = 0.65;
    static const double map_component_weight = 0.35;
}