import 'package:flutter/material.dart';

class MapSession extends StatelessWidget {
    const MapSession({super.key});

    @override
    Widget build(BuildContext context) {
        return Container(
            color: Colors.green,
            child: Text('Map Session'),
        );
    }
}